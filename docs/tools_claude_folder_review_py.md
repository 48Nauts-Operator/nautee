<!-- Auto-generated by Claude on 2025-06-22 03:25 -->

# Claude Folder Reviewer

## Overview

The **Claude Folder Reviewer** is a Python script that automates code review processes by scanning source code folders and leveraging Anthropic's Claude AI to generate comprehensive structural insights. The tool batches multiple source files into a single prompt and returns detailed analysis in Markdown format.

## Purpose

This script is designed to help developers and teams by:

- **Automated Code Analysis**: Scanning entire codebases for structural issues
- **Architecture Review**: Identifying weaknesses in code organization
- **Bug Detection**: Finding potential bug patterns across multiple files
- **Improvement Suggestions**: Providing recommendations for better modularization and code clarity

## Key Features

- 🔍 **Recursive File Scanning**: Automatically discovers source files in nested directories
- 📊 **Progress Tracking**: Real-time progress bar with ETA calculations  
- 🤖 **AI-Powered Analysis**: Uses Claude AI for intelligent code review
- 📝 **Markdown Output**: Generates well-formatted review reports
- ⚡ **Multi-Language Support**: Handles Python, JavaScript, TypeScript, and React files

## Functions

### `render_progress(current, total, width=30)`

Creates a visual progress bar for tracking file processing.

**Parameters:**
- `current` (int): Current number of processed files
- `total` (int): Total number of files to process
- `width` (int): Character width of the progress bar (default: 30)

**Returns:**
- `str`: Formatted progress bar string with percentage

```python
# Example output: [████████████----------] 67%
```

### `estimate_valid_files(folder, valid_exts)`

Recursively searches a directory tree for source files with valid extensions.

**Parameters:**
- `folder` (str): Root directory path to scan
- `valid_exts` (tuple): Tuple of acceptable file extensions

**Returns:**
- `list`: List of absolute file paths for all discovered source files

### `main()`

The primary execution function that orchestrates the entire review process:

1. **Environment Setup**: Loads API credentials and configuration
2. **File Discovery**: Scans target folder for source files
3. **Content Aggregation**: Reads and combines all source files
4. **AI Analysis**: Sends combined content to Claude for review
5. **Report Generation**: Saves the analysis to a timestamped Markdown file

## Configuration

### Environment Variables

Create a `.env` file with the following variables:

```env
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514
```

### Supported File Extensions

The script currently processes files with these extensions:
- `.py` (Python)
- `.js` (JavaScript)  
- `.ts` (TypeScript)
- `.tsx` (TypeScript React)
- `.jsx` (JavaScript React)

## Usage

### Basic Usage

```bash
python claude_folder_reviewer.py [folder_path]
```

### Examples

```bash
# Review current directory
python claude_folder_reviewer.py .

# Review specific folder
python claude_folder_reviewer.py /path/to/your/project

# Default behavior (reviews ../137docs)
python claude_folder_reviewer.py
```

## Output

The script generates timestamped Markdown files in the `output/` directory:

```
output/folder_review_2024-01-15T14-30-45.md
```

Each report includes:
- Code organization analysis
- Identified bug patterns
- Architecture weakness assessment
- Modularization recommendations
- Code clarity suggestions

## Dependencies

```python
pip install anthropic python-dotenv
```

## Notes and Suggestions

### ⚠️ Important Considerations

- **API Costs**: Be mindful of token usage when processing large codebases
- **File Size Limits**: Large files may exceed Claude's token limits
- **Rate Limiting**: Consider adding delays for very large projects

### 🔧 Potential Improvements

1. **Token Management**: Implement chunking for large codebases
2. **Configuration File**: Add support for custom file extensions and exclusions
3. **Filtering Options**: Add ability to exclude certain directories (e.g., `node_modules`, `.git`)
4. **Output Formats**: Support for additional output formats (JSON, HTML)
5. **Incremental Reviews**: Cache and only review changed files

### 🐛 Error Handling

The script includes basic error handling for:
- Missing or invalid files
- API connection issues
- File permission problems

### 💡 Best Practices

- Test with smaller folders first to understand token consumption
- Review the generated reports thoroughly as AI suggestions may need human validation
- Keep your API key secure and never commit it to version control
- Consider setting up file size limits for very large codebases