<!-- Auto-generated by Claude on 2025-06-22 13:27 -->

# Claude Folder Reviewer Documentation

## Overview

The **Claude Folder Reviewer** is a Python script that automatically analyzes codebases by scanning folders for source files and leveraging Anthropic's Claude AI to provide comprehensive code review insights. The tool generates structured feedback in Markdown format, focusing on code organization, potential bugs, architectural issues, and improvement suggestions.

## Purpose

This tool is designed for developers and teams who want to:
- Get automated code reviews of entire project folders
- Identify architectural weaknesses and bug patterns
- Receive suggestions for better code organization and modularization
- Generate documentation-ready review reports

## Key Features

- **Multi-language support**: Scans Python, JavaScript, TypeScript, and JSX/TSX files
- **Recursive folder scanning**: Processes entire directory structures
- **Progress tracking**: Visual progress bar with ETA calculations
- **Error handling**: Graceful handling of file read errors
- **Timestamped output**: Generates uniquely named review files

## Functions

### `render_progress(current, total, width=30)`

Creates a visual progress bar for tracking file processing.

**Parameters:**
- `current` (int): Current number of processed files
- `total` (int): Total number of files to process
- `width` (int): Character width of the progress bar (default: 30)

**Returns:**
- `str`: Formatted progress bar string with percentage

```python
# Example output: [████████████----------] 60%
```

### `estimate_valid_files(folder, valid_exts)`

Recursively searches a directory for files with specified extensions.

**Parameters:**
- `folder` (str): Root directory path to search
- `valid_exts` (tuple): Tuple of acceptable file extensions

**Returns:**
- `list`: List of full file paths matching the criteria

### `main()`

The primary function that orchestrates the entire review process:

1. **Environment setup**: Loads API keys and configuration
2. **File discovery**: Scans the target folder for source files
3. **Content aggregation**: Combines all source files into a single prompt
4. **AI analysis**: Sends the combined content to Claude for review
5. **Output generation**: Saves the review to a timestamped Markdown file

## Configuration

### Environment Variables

The script requires the following environment variables (typically stored in a `.env` file):

```bash
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Optional, has default
```

### Supported File Extensions

Currently supports:
- `.py` (Python)
- `.js` (JavaScript)
- `.ts` (TypeScript)
- `.tsx` (TypeScript JSX)
- `.jsx` (JavaScript JSX)

## Usage

### Command Line

```bash
# Review default folder (../137docs)
python claude_folder_reviewer.py

# Review specific folder
python claude_folder_reviewer.py /path/to/your/project
```

### Output

Reviews are saved to the `output/` directory with timestamped filenames:
```
output/folder_review_2024-01-15T14-30-45.md
```

## Dependencies

```bash
pip install anthropic python-dotenv
```

## Notes and Suggestions

### ⚠️ Important Considerations

- **API Costs**: Each review consumes Claude API tokens. Monitor usage for large codebases
- **File Size Limits**: Very large codebases may exceed Claude's token limits
- **Sensitive Code**: Ensure you're comfortable sending your code to Anthropic's servers

### 🔧 Potential Improvements

1. **Token Management**: Add token counting and chunking for large codebases
2. **File Filtering**: Add configuration options to exclude specific files or directories
3. **Output Formats**: Support additional output formats (JSON, HTML)
4. **Incremental Reviews**: Only review changed files since last run
5. **Custom Prompts**: Allow users to customize the review focus areas

### 🐛 Error Handling

The script includes basic error handling for:
- Missing or invalid API keys
- File read permissions
- Empty directories
- API request failures

### 📁 Example Directory Structure

```
your-project/
├── src/
│   ├── main.py
│   ├── utils.js
│   └── components/
│       └── Button.tsx
└── claude_folder_reviewer.py
output/
└── folder_review_2024-01-15T14-30-45.md
```

## Security Notes

- Store API keys securely in environment variables
- Consider excluding sensitive files or directories
- Review the generated output before sharing, as it may contain code snippets