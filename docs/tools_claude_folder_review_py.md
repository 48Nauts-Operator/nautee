<!-- Auto-generated by Claude on 2025-06-02 01:47 -->

# Claude Folder Reviewer Documentation

## Overview

The **Claude Folder Reviewer** is a Python script that automatically analyzes entire codebases by scanning directories for source files and leveraging Anthropic's Claude AI to provide comprehensive code review insights. The tool generates structured feedback in Markdown format, focusing on code organization, potential bugs, architectural issues, and improvement suggestions.

## Purpose

This tool is designed to:
- **Automate code reviews** for entire project folders
- **Identify structural issues** and architectural weaknesses  
- **Provide actionable insights** for code improvement
- **Generate comprehensive reports** in readable Markdown format
- **Save time** on manual code review processes

## Key Features

- 🔍 **Recursive file scanning** with support for multiple programming languages
- 📊 **Real-time progress tracking** with visual progress bars and ETA estimates
- 🤖 **AI-powered analysis** using Anthropic Claude models
- 📝 **Markdown output** with timestamped reports
- ⚙️ **Configurable** via environment variables

## Dependencies

```python
import os
import sys
import anthropic
from dotenv import load_dotenv
from datetime import datetime
import time
```

### Required Packages
- `anthropic` - Anthropic Claude API client
- `python-dotenv` - Environment variable management

## Important Functions

### `render_progress(current, total, width=30)`

Creates a visual progress bar for tracking file processing.

**Parameters:**
- `current` (int): Current progress count
- `total` (int): Total items to process  
- `width` (int): Progress bar width in characters (default: 30)

**Returns:**
- `str`: Formatted progress bar string with percentage

```python
# Example output: [████████████----------] 67%
```

### `estimate_valid_files(folder, valid_exts)`

Recursively discovers all source files with specified extensions in a directory tree.

**Parameters:**
- `folder` (str): Root directory to search
- `valid_exts` (tuple): Acceptable file extensions

**Returns:**
- `list`: Full file paths of discovered source files

### `main()`

The primary execution function that orchestrates the entire review process:

1. **Environment Setup** - Loads API keys and configuration
2. **File Discovery** - Scans for valid source files
3. **Content Aggregation** - Reads and combines file contents
4. **Progress Tracking** - Displays real-time progress with ETA
5. **AI Analysis** - Sends combined code to Claude for review
6. **Report Generation** - Saves timestamped Markdown output

## Configuration

### Environment Variables

Create a `.env` file with the following variables:

```bash
ANTHROPIC_API_KEY=your_claude_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Optional, has default
```

### Supported File Extensions

Currently supports:
- `.py` (Python)
- `.js` (JavaScript) 
- `.ts` (TypeScript)
- `.tsx` (TypeScript React)
- `.jsx` (JavaScript React)

## Usage

### Basic Usage
```bash
python claude_folder_reviewer.py
```
*Uses default folder: `../137docs`*

### Specify Target Folder
```bash
python claude_folder_reviewer.py /path/to/your/project
```

## Output

### Console Output
- Real-time progress bar with ETA estimates
- File-by-file processing status
- Summary of reviewed files
- Total processing time

### Generated Report
- **Location**: `output/folder_review_YYYY-MM-DDTHH-MM-SS.md`
- **Format**: Structured Markdown with AI insights
- **Content**: Code organization analysis, bug patterns, architectural recommendations

## Notes and Suggestions

### ⚠️ Important Considerations

1. **API Rate Limits**: Be mindful of Anthropic's API rate limits when processing large codebases
2. **Token Limits**: The script uses a 2000 token limit for responses - consider increasing for larger projects
3. **File Size**: Very large codebases may exceed Claude's input token limits

### 🔧 Potential Improvements

1. **Chunking Strategy**: Implement intelligent code chunking for very large projects
2. **File Filtering**: Add options to exclude specific directories (e.g., `node_modules`, `.git`)
3. **Output Formats**: Support additional output formats (JSON, HTML)
4. **Configuration File**: Add support for configuration files beyond environment variables
5. **Language Detection**: Automatic detection of programming languages for better analysis

### 💡 Best Practices

- **Regular Reviews**: Run on feature branches before merging
- **Incremental Analysis**: Focus on changed files for faster feedback
- **Team Integration**: Incorporate into CI/CD pipelines for automated reviews
- **Archive Reports**: Keep historical reviews to track code quality over time

### 🚨 Error Handling

The script includes basic error handling for:
- Missing or invalid files
- API connection issues  
- File permission problems
- Empty directories

Consider enhancing error handling for production use cases.