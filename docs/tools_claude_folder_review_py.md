<!-- Auto-generated by Claude on 2025-06-01 10:20 -->

# Claude Folder Reviewer Documentation

## Overview

The **Claude Folder Reviewer** is a Python script that automatically analyzes entire codebases by scanning source files in a directory and using Anthropic's Claude AI to generate comprehensive code review insights. The tool provides structural analysis, identifies potential issues, and offers improvement suggestions in Markdown format.

## Purpose

This tool is designed to help developers and teams:
- Get automated code reviews for entire project folders
- Identify architectural weaknesses and bug patterns
- Receive suggestions for better code organization and modularization
- Generate comprehensive documentation of code quality insights

## Key Features

- **Recursive file scanning** of directories
- **Batch processing** of multiple source files
- **Progress tracking** with visual progress bar and ETA
- **Automated AI-powered code analysis** using Claude
- **Markdown output** for easy integration into documentation

## Functions

### `render_progress(current: int, total: int, width: int = 30) -> str`

Generates a visual progress bar for tracking file processing.

**Parameters:**
- `current`: Number of files processed so far
- `total`: Total number of files to process
- `width`: Character width of the progress bar (default: 30)

**Returns:** Formatted progress bar string with percentage

**Example output:**
```
[████████████████------] 67%
```

### `estimate_valid_files(folder: str, valid_exts: tuple) -> list`

Recursively scans a directory to find all source files with valid extensions.

**Parameters:**
- `folder`: Root directory path to search
- `valid_exts`: Tuple of acceptable file extensions

**Returns:** List of full file paths matching the criteria

### `main()`

The primary function that orchestrates the entire review process:

1. **Environment Setup**: Loads API keys and configuration
2. **File Discovery**: Scans for valid source files
3. **Content Aggregation**: Reads and combines file contents
4. **AI Analysis**: Sends combined content to Claude for review
5. **Output Generation**: Saves results to timestamped Markdown file

## Configuration

### Environment Variables

Create a `.env` file with the following variables:

```env
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514
```

### Supported File Types

The script currently processes files with these extensions:
- `.py` (Python)
- `.js` (JavaScript)
- `.ts` (TypeScript)
- `.tsx` (TypeScript React)
- `.jsx` (JavaScript React)

## Usage

### Basic Usage

```bash
python claude_folder_reviewer.py [folder_path]
```

### Examples

```bash
# Review current directory's parent folder
python claude_folder_reviewer.py ../my_project

# Review specific project folder
python claude_folder_reviewer.py /path/to/my/codebase

# Use default folder (../137docs)
python claude_folder_reviewer.py
```

## Output

The script generates:
- **Console output**: Real-time progress tracking and file listing
- **Markdown file**: Comprehensive review saved to `output/folder_review_YYYY-MM-DDTHH-MM-SS.md`

### Sample Console Output

```
🔍 Found 15 source files in: ../my_project

[████████████████------] 67% ⏱ ETA: 02:30 | ✅ src/main.py
[██████████████████----] 75% ⏱ ETA: 01:45 | ✅ src/utils.py

✅ Folder review saved to `output/folder_review_2024-01-15T14-30-25.md` in 05:32

📁 Files Reviewed:
  - src/main.py
  - src/utils.py
  - tests/test_main.py
```

## Dependencies

```python
import os
import sys
import anthropic
from dotenv import load_dotenv
from datetime import datetime
import time
```

**Required packages:**
```bash
pip install anthropic python-dotenv
```

## Notes and Suggestions

### ⚠️ Important Considerations

- **API Costs**: Be mindful of token usage when processing large codebases
- **File Size Limits**: Very large files may exceed Claude's context window
- **Rate Limiting**: Consider adding delays for large projects to avoid API limits

### 🔧 Potential Improvements

1. **Add file filtering options** to exclude specific directories (e.g., `node_modules`, `.git`)
2. **Implement chunking strategy** for very large codebases
3. **Add configuration file support** for customizable file extensions
4. **Include error recovery** and retry logic for API failures
5. **Add output format options** (JSON, HTML, etc.)

### 💡 Usage Tips

- Start with smaller folders to test API integration
- Review the generated Markdown files for actionable insights
- Use the tool regularly during development cycles for continuous code quality monitoring
- Consider integrating into CI/CD pipelines for automated code reviews

## Error Handling

The script includes basic error handling for:
- **File reading errors**: Continues processing other files
- **API errors**: Reports Claude API issues
- **Missing files**: Validates file existence before processing