<!-- Auto-generated by Claude on 2025-06-01 23:18 -->

# Autodoc Generator Documentation

<!-- Auto-generated by Claude on 2024-12-20 15:30 -->

## Overview

The `autodoc.py` script is an automated documentation generator that scans Python source files in a project and creates comprehensive Markdown documentation using Claude AI from Anthropic. It integrates seamlessly with MkDocs to maintain an organized documentation site.

## Purpose

This tool automates the documentation process by:
- **Scanning** Python files in a specified directory
- **Generating** detailed Markdown documentation using AI
- **Organizing** documentation files in a structured format
- **Updating** MkDocs configuration automatically
- **Creating** an index page with navigation links

## Key Features

### 🔍 Intelligent File Discovery
- Automatically finds Python files in the project
- Excludes test files and `__init__.py` files
- Supports recursive directory scanning

### 🤖 AI-Powered Documentation
- Uses Claude AI to generate comprehensive documentation
- Creates structured Markdown with headings, code blocks, and explanations
- Provides technical insights and usage suggestions

### 📚 MkDocs Integration
- Automatically updates `mkdocs.yml` configuration
- Creates organized navigation structure
- Supports Material theme with enhanced features

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to snake_case Markdown filenames:
```python
# Example: "src/utils/helper.py" → "src_utils_helper_py.md"
```

### `load_mkdocs_config(path: str)`
- Loads existing MkDocs configuration
- Creates default config if none exists
- Preserves existing navigation structure

### `write_index_md(nav_items)`
- Generates the main documentation index page
- Creates organized links to all documentation files
- Includes timestamp and portal branding

### `collect_python_files(base: str)`
- Recursively scans directories for Python files
- Filters out test files and init files
- Returns list of files to document

### `write_mkdocs_config(path: str, static_nav, autodoc_items)`
Writes comprehensive MkDocs configuration with:
- **Material theme** with dark/light mode toggle
- **Enhanced features**: search, navigation, code highlighting
- **Markdown extensions**: admonitions, syntax highlighting, emojis
- **Plugin support**: search and mkdocstrings

## Configuration

### Environment Variables
```bash
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Optional, defaults to specified model
```

### Command Line Usage
```bash
# Document current directory
python autodoc.py

# Document specific path
python autodoc.py /path/to/project
```

## Output Structure

```
docs/
├── index.md                    # Main documentation index
├── module_name_py.md          # Generated documentation files
├── utils_helper_py.md
└── ...

mkdocs.yml                     # Updated MkDocs configuration
```

## Technical Notes

### ⚠️ Important Considerations

- **API Costs**: Each file generates an API call to Claude - monitor usage for large projects
- **File Filtering**: Only processes `.py` files, excludes tests and `__init__.py`
- **Overwrite Behavior**: Regenerates documentation files completely on each run
- **Rate Limits**: No built-in rate limiting - consider adding delays for large projects

### 🔧 Customization Suggestions

1. **Add rate limiting** for processing many files
2. **Implement caching** to avoid regenerating unchanged files
3. **Add configuration file** for custom filtering rules
4. **Support multiple output formats** beyond Markdown
5. **Add progress bars** for better user experience

## Error Handling

The script includes robust error handling:
- Continues processing if individual files fail
- Provides clear error messages with file context
- Gracefully handles missing configuration files

## Dependencies

- `anthropic` - Claude AI API client
- `python-dotenv` - Environment variable management
- `pyyaml` - YAML configuration handling

This tool streamlines the documentation process and ensures consistent, comprehensive documentation across Python projects.