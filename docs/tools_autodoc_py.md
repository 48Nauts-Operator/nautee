<!-- Auto-generated by Claude on 2025-06-01 16:23 -->

# autodoc.py Documentation

<!-- Auto-generated by Claude on 2024-12-28 -->

## Overview

**autodoc.py** is an automated documentation generator that scans Python source files in a project and creates comprehensive Markdown documentation using Claude AI from Anthropic. The tool integrates seamlessly with MkDocs to create a complete documentation website.

## Purpose

This script automates the tedious process of writing technical documentation by:
- Scanning project directories for Python files
- Generating detailed Markdown documentation using AI
- Creating and maintaining a MkDocs-compatible documentation site
- Automatically updating navigation and index files

## Key Features

### 🤖 AI-Powered Documentation Generation
- Uses Claude AI to analyze Python source code
- Generates comprehensive technical documentation
- Explains code purpose, functions, classes, and provides suggestions

### 📁 Automatic Project Scanning
- Recursively scans directories for Python files
- Filters out test files and `__init__.py` files
- Processes multiple files in batch

### 🌐 MkDocs Integration
- Automatically generates and updates `mkdocs.yml` configuration
- Creates navigation structure with Material theme
- Maintains existing manual documentation alongside auto-generated content

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to snake_case Markdown filenames:
```python
# Example: "src/utils/helper.py" → "src_utils_helper_py.md"
```

### `load_mkdocs_config(path: str)`
- Loads existing MkDocs configuration or creates default structure
- Preserves manual navigation entries
- Returns dictionary with site configuration

### `write_index_md(nav_items)`
- Creates main documentation index page
- Lists all auto-generated documentation files
- Includes timestamp and welcome message

### `write_mkdocs_config(path, static_nav, autodoc_items)`
- Updates MkDocs configuration with comprehensive Material theme setup
- Includes advanced features like syntax highlighting, search, and dark/light mode
- Merges static navigation with auto-generated documentation

### `collect_python_files(base: str)`
- Recursively finds all Python files in target directory
- Excludes test files and `__init__.py` files
- Returns list of file paths for processing

## Configuration

### Environment Variables
- `ANTHROPIC_API_KEY`: Required API key for Claude AI access
- `ANTHROPIC_MODEL`: Optional model specification (defaults to "claude-sonnet-4-20250514")

### Command Line Usage
```bash
python autodoc.py [target_directory]
```
- `target_directory`: Optional path to scan (defaults to current directory)

## Output Structure

```
docs/
├── index.md                    # Main documentation index
├── module_name_py.md          # Auto-generated docs for each Python file
└── [other existing .md files] # Preserved manual documentation

mkdocs.yml                     # Updated MkDocs configuration
```

## Notes and Suggestions

### ⚠️ Prerequisites
- Requires `anthropic`, `python-dotenv`, and `pyyaml` packages
- Must have valid Anthropic API key configured
- Designed for Python projects with standard structure

### 💡 Best Practices
- Run from project root directory for best results
- Review generated documentation before publishing
- Consider running periodically to keep docs synchronized with code changes

### 🔧 Customization Options
- Modify the AI prompt to adjust documentation style
- Update MkDocs theme configuration in `write_mkdocs_config()`
- Adjust file filtering logic in `collect_python_files()`

### 🚨 Error Handling
- Gracefully handles missing files or API errors
- Continues processing remaining files if individual files fail
- Provides detailed console output for debugging

## Theme Features

The generated MkDocs site includes:
- **Material Design** with dark/light mode toggle
- **Advanced Navigation** with instant loading and tabs
- **Code Highlighting** with line numbers and copy buttons
- **Search Functionality** with suggestions and highlighting
- **Markdown Extensions** for enhanced formatting and features

This tool significantly reduces the maintenance burden of keeping project documentation up-to-date while ensuring consistent, comprehensive coverage of your Python codebase.