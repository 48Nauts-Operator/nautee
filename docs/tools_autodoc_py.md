<!-- Auto-generated by Claude on 2025-06-21 22:18 -->

# Autodoc.py Documentation

<!-- Auto-generated by Claude on 2024-01-XX XX:XX -->

## Overview

**autodoc.py** is an automated documentation generator that scans Python source files in a project and creates comprehensive Markdown documentation using Claude AI. The script integrates with MkDocs to create a complete documentation website with proper navigation structure.

## Purpose

This tool serves as a documentation automation solution that:
- Scans project directories for Python files
- Generates detailed Markdown documentation using Claude AI
- Maintains MkDocs configuration with proper navigation
- Creates an organized documentation portal

## Key Features

- **Automated Python file discovery**: Recursively scans directories for `.py` files
- **AI-powered documentation**: Uses Claude to generate comprehensive docs
- **MkDocs integration**: Automatically updates `mkdocs.yml` configuration
- **Index generation**: Creates and maintains documentation index pages
- **Material theme support**: Configured with Material theme and extensions

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to Markdown-friendly filenames by replacing path separators and dots with underscores.

```python
# Example: "src/utils.py" becomes "src_utils_py.md"
```

### `load_mkdocs_config(path: str)`
Loads existing MkDocs configuration or creates a default structure with Material theme settings.

### `write_index_md(nav_items)`
Generates the main documentation index page with:
- Welcome message and branding
- Links to all auto-generated documentation
- Timestamp for last update

### `write_mkdocs_config(path: str, static_nav, autodoc_items)`
Creates comprehensive MkDocs configuration including:
- Material theme with dark/light mode toggle
- Extensive markdown extensions (syntax highlighting, admonitions, etc.)
- Search functionality and navigation features
- Plugin configuration for mkdocstrings

### `collect_python_files(base: str)`
Recursively discovers Python files while filtering out:
- Test files (`test_*.py`)
- `__init__.py` files
- Other non-documentation worthy files

## Configuration Requirements

### Environment Variables
- `ANTHROPIC_API_KEY`: Required API key for Claude access
- `ANTHROPIC_MODEL`: Optional model specification (defaults to `claude-sonnet-4-20250514`)

### Dependencies
- `anthropic`: Claude AI client
- `python-dotenv`: Environment variable management
- `pyyaml`: YAML configuration handling

## Usage

```bash
# Document current directory
python autodoc.py

# Document specific path
python autodoc.py /path/to/project
```

## Output Structure

```
docs/
├── index.md                 # Main documentation index
├── filename1_py.md         # Generated docs for each Python file
├── filename2_py.md
└── ...
mkdocs.yml                  # Updated MkDocs configuration
```

## Notes and Suggestions

### ✅ Strengths
- **Comprehensive automation**: Handles entire documentation pipeline
- **Professional theming**: Well-configured Material theme
- **Error handling**: Graceful handling of processing failures
- **Flexible input**: Accepts command-line path arguments

### ⚠️ Considerations
- **API costs**: Each file processed requires a Claude API call
- **Rate limiting**: No built-in rate limiting for large projects
- **File filtering**: May need adjustment for different project structures
- **Token limits**: 1500 token limit may be insufficient for very large files

### 🔧 Potential Improvements
- Add rate limiting for API calls
- Implement caching to avoid re-processing unchanged files
- Add configuration file support for customizing filters and settings
- Include progress bars for large projects
- Add dry-run mode for testing

### 💡 Best Practices
- Run on a schedule to keep documentation current
- Review generated documentation for accuracy
- Maintain separate manual documentation for high-level concepts
- Consider integrating with CI/CD pipelines for automatic updates