<!-- Auto-generated by Claude on 2025-06-23 03:26 -->

# autodoc.py Documentation

<!-- Auto-generated by Claude on 2024-12-19 -->

## Overview

`autodoc.py` is an **automated documentation generator** that scans Python source files in a project and generates comprehensive Markdown documentation using Claude AI. It integrates seamlessly with MkDocs by automatically updating the `mkdocs.yml` configuration file.

## Purpose

- **Automate documentation generation** for Python projects
- **Reduce manual documentation overhead** by leveraging AI
- **Maintain up-to-date documentation** that syncs with code changes
- **Integrate with MkDocs** for professional documentation sites

## Key Features

### 🔍 Smart File Discovery
- Recursively scans directories for Python files
- Excludes test files (`test_*.py`) and `__init__.py` files
- Processes only relevant source code files

### 🤖 AI-Powered Documentation
- Uses Claude AI (Anthropic) to generate technical documentation
- Creates structured Markdown with headings, bullet points, and code blocks
- Provides explanations of purpose, functions, classes, and usage suggestions

### 📚 MkDocs Integration
- Automatically updates `mkdocs.yml` configuration
- Preserves existing navigation structure
- Adds generated docs under "AutoDocs" section
- Includes comprehensive Material theme configuration

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to snake_case Markdown filenames.
```python
# Example: "src/utils/helper.py" → "src_utils_helper_py.md"
```

### `load_mkdocs_config(path: str)`
- Loads existing MkDocs configuration
- Returns default config if none exists
- Preserves user customizations

### `write_mkdocs_config(path: str, static_nav, autodoc_items)`
- Writes comprehensive MkDocs configuration
- Includes Material theme with dark/light mode toggle
- Configures markdown extensions and plugins
- Combines static navigation with auto-generated docs

### `collect_python_files(base: str)`
- Recursively finds Python source files
- Filters out test files and `__init__.py`
- Returns list of file paths for processing

## Configuration

### Required Environment Variables
```bash
ANTHROPIC_API_KEY=your_claude_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Optional, defaults to this model
```

### Usage
```bash
# Scan current directory
python autodoc.py

# Scan specific directory
python autodoc.py /path/to/project
```

## Output Structure

```
docs/
├── index.md                    # Auto-generated if missing
├── module_name_py.md          # Generated docs for each Python file
└── other_existing_docs.md     # Preserved existing documentation

mkdocs.yml                     # Updated with AutoDocs navigation
```

## MkDocs Theme Features

The generated `mkdocs.yml` includes:

- **Material Design** theme with modern UI
- **Dark/Light mode** toggle
- **Code syntax highlighting** with line numbers
- **Search functionality** with suggestions
- **Navigation enhancements** (instant loading, tabs, top navigation)
- **Content features** (copy code buttons, edit actions)
- **Markdown extensions** (admonitions, emojis, task lists)

## Notes and Suggestions

### ✅ Strengths
- **Fully automated** documentation pipeline
- **Preserves manual documentation** while adding auto-generated content
- **Professional output** with Material theme
- **Error handling** for individual file processing failures

### ⚠️ Considerations
- **API costs**: Each file requires a Claude API call
- **Rate limits**: May need throttling for large projects
- **Quality variation**: AI-generated docs may need manual review
- **Dependency**: Requires stable internet connection and API access

### 🚀 Potential Improvements
- Add **batch processing** to reduce API calls
- Implement **caching** to avoid regenerating unchanged files
- Add **configuration file** for customizing output templates
- Include **diff detection** to only update modified files
- Add **dry-run mode** for testing without API calls

### 📋 Dependencies
```python
anthropic      # Claude AI API client
python-dotenv  # Environment variable management
pyyaml        # YAML configuration handling
```

This tool is ideal for teams wanting to maintain comprehensive, up-to-date documentation with minimal manual effort while leveraging the power of AI for technical writing.