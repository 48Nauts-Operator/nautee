<!-- Auto-generated by Claude on 2025-06-23 06:38 -->

# Autodoc Generator Documentation

<!-- Auto-generated documentation for autodoc.py -->

## Overview

The **Autodoc Generator** is an automated documentation tool that scans Python projects, generates comprehensive Markdown documentation using Claude AI, and integrates the results into a MkDocs-compatible documentation structure.

## Purpose

This script automates the tedious process of writing technical documentation by:
- Scanning project directories for Python source files
- Using Claude AI to generate high-quality Markdown documentation
- Organizing documentation files in a structured format
- Automatically updating MkDocs configuration for seamless integration

## Key Features

### 🔍 **Intelligent File Discovery**
- Recursively scans directories for Python files
- Excludes test files and `__init__.py` files automatically
- Processes files in batch for efficient documentation generation

### 🤖 **AI-Powered Documentation**
- Leverages Anthropic's Claude AI for natural language documentation
- Generates structured Markdown with headings, bullet points, and code blocks
- Provides explanations of purpose, functions, classes, and usage suggestions

### 📚 **MkDocs Integration**
- Automatically updates `mkdocs.yml` configuration
- Preserves existing navigation structure
- Adds generated docs under an "AutoDocs" section

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to snake_case Markdown filenames.
```python
# Example: "src/utils/helper.py" → "src_utils_helper_py.md"
```

### `load_mkdocs_config(path: str)`
Loads existing MkDocs configuration or creates a default structure with Material theme.

### `write_mkdocs_config(path: str, static_nav, autodoc_items)`
Writes comprehensive MkDocs configuration with:
- Material theme with dark/light mode toggle
- Enhanced markdown extensions
- Search and navigation features
- Code highlighting and copy functionality

### `collect_python_files(base: str)`
Recursively discovers Python files while filtering out:
- Test files (`test_*.py`)
- Init files (`__init__.py`)
- Non-Python files

## Configuration

### Environment Variables
- `ANTHROPIC_API_KEY`: Required API key for Claude AI
- `ANTHROPIC_MODEL`: AI model to use (default: "claude-sonnet-4-20250514")

### Command Line Usage
```bash
python autodoc.py [target_directory]
```
- `target_directory`: Optional path to scan (defaults to current directory)

## Output Structure

```
docs/
├── index.md                    # Auto-generated index
├── module_name_py.md          # Generated documentation files
└── ...
mkdocs.yml                     # Updated configuration
```

## Notes and Suggestions

### ✅ **Strengths**
- Comprehensive MkDocs theme configuration with modern features
- Robust error handling for file processing
- Preserves existing documentation structure
- Detailed logging and progress feedback

### ⚠️ **Considerations**
- **API Costs**: Each file generates an API call to Claude - monitor usage for large projects
- **Rate Limiting**: Consider adding delays for projects with many files
- **File Size Limits**: Large Python files may exceed Claude's token limits
- **Dependency Management**: Requires `anthropic`, `python-dotenv`, and `PyYAML` packages

### 🔧 **Recommended Improvements**
1. Add configuration file support for customizable settings
2. Implement batch processing to reduce API calls
3. Add support for excluding specific directories or files
4. Include file modification date checking to avoid regenerating unchanged files
5. Add validation for generated documentation quality

### 📋 **Prerequisites**
```bash
pip install anthropic python-dotenv PyYAML
```

### 🚀 **Usage Example**
```bash
# Set your API key
export ANTHROPIC_API_KEY="your-api-key-here"

# Generate docs for current directory
python autodoc.py

# Generate docs for specific directory
python autodoc.py ./src
```

The tool will create a professional documentation site ready for deployment with `mkdocs serve` or `mkdocs build`.