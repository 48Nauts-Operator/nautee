<!-- Auto-generated by Claude on 2025-06-02 06:37 -->

# autodoc.py Documentation

<!-- Auto-generated by Claude on 2024-12-19 -->

## Overview

`autodoc.py` is an automated documentation generator that scans Python projects and creates comprehensive Markdown documentation using Claude AI. The script integrates with MkDocs to provide a complete documentation pipeline for Python codebases.

## Purpose

This tool automates the tedious process of writing technical documentation by:
- Scanning Python source files in a project
- Generating detailed Markdown documentation using Claude AI
- Organizing documentation files in a `docs/` directory
- Automatically updating MkDocs configuration for seamless integration

## Key Features

### 🔍 **Intelligent File Discovery**
- Recursively scans directories for Python files
- Filters out test files (`test_*.py`) and `__init__.py` files
- Processes only relevant source code files

### 🤖 **AI-Powered Documentation Generation**
- Uses Anthropic's Claude API for natural language documentation
- Generates comprehensive explanations of code purpose and functionality
- Creates structured Markdown with proper formatting

### 📚 **MkDocs Integration**
- Automatically updates `mkdocs.yml` configuration
- Preserves existing navigation structure
- Adds generated docs under an "AutoDocs" section

## Important Functions

### `snake_md_path(rel_path: str) -> str`
Converts file paths to documentation-friendly Markdown filenames by replacing path separators and dots with underscores.

```python
# Example: "src/utils/helper.py" → "src_utils_helper_py.md"
```

### `load_mkdocs_config(path: str)`
Safely loads existing MkDocs configuration or creates a default structure if none exists.

### `write_mkdocs_config(path: str, static_nav, autodoc_items)`
Writes a comprehensive MkDocs configuration with:
- Material theme with dark/light mode toggle
- Enhanced markdown extensions
- Search functionality
- Code highlighting and copying features

### `collect_python_files(base: str)`
Recursively discovers Python files while excluding test files and init files.

## Configuration

### Environment Variables
- `ANTHROPIC_API_KEY`: Required API key for Claude AI service
- `ANTHROPIC_MODEL`: AI model to use (defaults to "claude-sonnet-4-20250514")

### Command Line Usage
```bash
python autodoc.py [target_directory]
```
If no directory is specified, defaults to current directory (`.`).

## Generated Output Structure

```
docs/
├── index.md                    # Auto-generated index page
├── filename1_py.md            # Documentation for filename1.py
├── subfolder_filename2_py.md  # Documentation for subfolder/filename2.py
└── ...
```

## MkDocs Features Configured

The script automatically configures MkDocs with advanced features:

- **Theme**: Material Design with professional styling
- **Navigation**: Instant loading, tabs, and top navigation
- **Code Features**: Syntax highlighting, line numbers, copy buttons
- **Search**: Enhanced search with suggestions and highlighting
- **Extensions**: Admonitions, footnotes, task lists, and emoji support

## Notes and Suggestions

### ⚠️ **Prerequisites**
- Ensure `ANTHROPIC_API_KEY` is set in your environment or `.env` file
- Install required dependencies: `anthropic`, `python-dotenv`, `pyyaml`

### 💡 **Best Practices**
- Run this script from your project root directory
- Review generated documentation for accuracy before publishing
- Consider running periodically to keep docs synchronized with code changes

### 🔧 **Customization Options**
- Modify the AI prompt template to adjust documentation style
- Update the MkDocs theme configuration to match your branding
- Add custom markdown extensions as needed

### ⚡ **Performance Considerations**
- Large projects may take time due to API calls for each file
- Consider implementing caching for unchanged files in future versions
- Monitor API usage to stay within rate limits

## Error Handling

The script includes robust error handling:
- Gracefully handles missing configuration files
- Continues processing other files if one fails
- Provides clear status messages and error reporting

This tool significantly reduces the manual effort required to maintain up-to-date project documentation while ensuring consistency and comprehensiveness across your Python codebase.